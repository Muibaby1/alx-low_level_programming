#!/bin/bash
#include <stdio.h>

int main(void)
{
		/*
		 * 	 * function to output a text string using 
		 * 	 	 * printf function.
		 * 	 	 	 *
		 * 	 	 	 	 */
		printf("with proper grammar, but the outcome is a piece of art,");
			return (0);
}
